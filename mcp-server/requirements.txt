# MCP Server Dependencies
# Pinned versions for deterministic builds and Pydantic 2.12+ compatibility
# FastMCP 2.14.5 resolves Pydantic 2.8+ compatibility issues from 0.2.0
fastmcp==2.14.5
mcp==1.26.0
pydantic==2.12.5

# Template rendering
jinja2==3.1.4
pyyaml==6.0.2

# Web server (let FastMCP determine compatible version)
uvicorn[standard]>=0.35

# Testing
pytest>=8.3
pytest-asyncio>=0.24
httpx>=0.28.1
