# Database
DATABASE_URL=mysql://hellio:helliopassword@localhost:3306/hellio_hr

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=24h

# Server
PORT=3000
NODE_ENV=development

# Agent User (used during database seeding)
# This password is used for the agent@hellio.hr account
# IMPORTANT: Change this to a strong password before production
AGENT_PASSWORD=<CHANGE_ME_BEFORE_PRODUCTION>
